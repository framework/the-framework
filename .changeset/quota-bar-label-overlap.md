---
'@gemstack/framework-dashboard': patch
---

Fixed the usage bar's day labels overlapping when the quota week starts mid-day (#960): the label for the first, partial day sat almost on top of the label beside it — now it drops to a line of its own instead.
