---
'@gemstack/the-framework': patch
---

The build prompt now asks for the `set-scope` verdict (#1356) — a trivial run has no sizing moment of its own, so the optional protocol block went unemitted and the full checklist still ran.
