---
"@gemstack/the-framework": minor
---

Tickets are now their own full page (#1144) instead of a tab squeezed into the 27rem right rail. Reach it from a new "Tickets" row in the sidebar, under New session, once a project is selected. The list shows each ticket as a one-liner; clicking one opens its own page at `/{projectId}/tickets/{filename}`, showing the ticket's entire markdown (not just the head) and where the Queue button now lives. The empty-backlog GitHub import stays on the list.
