---
"@gemstack/the-framework": minor
---

Tickets are now their own full page (#1144) instead of a tab squeezed into the 27rem right rail. Reach it from a new "Tickets" row in the sidebar, under New session, once a project is selected — the same list, queue button, and GitHub import as before, just readable at full width.
