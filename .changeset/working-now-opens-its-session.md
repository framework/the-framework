---
"@gemstack/the-framework": patch
---

Open the session a row on the Overview is describing. "Working now" rows, and hot tickets a run is implementing, carried the run they named and then navigated by project alone, landing on that project's launcher instead of the session.
