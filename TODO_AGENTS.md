# TODO_AGENTS

## Priority 9

- [Dry-run the Human Queue demo flow and fix glaring breaks](tickets/2026-07-25_nice-demo-human-queue.md) — the maintainer hasn't tested the flow and needs it to demo well: click a Human Queue item → see the list of choices → pick one → done. Walk that exact path, fix only glaring breaks that would show on camera ("non-glaring paper cuts are okay" per the ticket), and report the state of the flow either way.

## Priority 8

- [Remove the "do NOT re-scaffold" babysitting paragraph from the system prompt](tickets/2026-07-26_remove-ugly-babysitting.md) — delete the paragraph at `packages/the-framework/src/steps.ts:62` and update the assertions in `packages/the-framework/src/steps.test.ts` (~lines 198 and 227) that match on its text.
- [Root-cause the white-screen crash behind the Serve menu item](tickets/2026-07-25_bug-white-blank.md) — deterministic repro confirmed by the maintainer: hovering the "Serve" menu item blanks the whole UI; #1196 landed as an explicit mitigation and the issue was deliberately kept open for the real fix. Start from the Serve state in `packages/framework-dashboard/components/SessionActionsMenu.tsx` and the #1196 mitigation diff, find the crash, fix its root cause; if it turns out architectural rather than a contained bug, report the diagnosis instead of forcing a fix.
- [Re-verify the ticket Queue button end-to-end and fix what still no-ops](tickets/2026-07-25_queue-button-doesnt-work.md) — clicking `Queue` on a ticket (`sendQueueTicket`, `packages/framework-dashboard/components/TicketDetailPage.tsx` → `packages/the-framework/src/queue-promote.ts`) appeared to do nothing beyond a small UI change and left the user asking "What should I do now?". The #1297 parser fix and #1304 queue-card work landed after the report, so first reproduce against current `main`; fix whatever still dead-ends, and make the queued state name what happens next (that the sweep will pick it up) — smallest change that removes the confusion.
- [Remove the "It's a folder of repos" option from the Add-project modal](tickets/2026-07-25_improve-add-project.md) — scoped to the decided half of the ticket only: the option's meaning is unclear to users and the maintainer said "let's remove it for now" (the proper directory-of-repos feature is post-MVP, #1115). The modal→system-file-picker replacement stays out of this entry — a localhost browser page cannot read absolute directory paths from the native picker, so that half needs a decision first.
- [Auto-open the session-actions ⋮ menu on hover](tickets/2026-07-25_improve-tooltip.md) — the ticket's remaining follow-up, proposed with "only if quick-win": open the dropdown (`packages/framework-dashboard/components/SessionActionsMenu.tsx`, the `MoreVertical` trigger) on pointer hover instead of requiring a click, keeping click and keyboard behavior working; bail out and report if it fights the dropdown primitives. The thread's settings-row redesign stays out — it overlaps the queued settle-moment entry.
- [Add the Auto-merge setting for routine-opened PRs](tickets/2026-07-26_auto-merge.md) — new setting `Auto-merge`, default false, per the ticket; when a run opens its PR (the handoff seam, `packages/the-framework/src/dashboard/run-handoff.ts` / `gh.ts`), a truthy setting enables GitHub auto-merge on that PR so it lands once checks pass, and the quick-win/consensual triage routines can set it true. Only the setting the ticket names — the `Auto commit`/`Auto push` split stays postponed per the settle-moment entry.

## Priority 7

- [Retry runs killed by transient API drops](tickets/2026-07-27_transient-api-drop-kills-run.md) — when a child exits nonzero with a driver-level transient error (connection drop, 5xx, rate-limit), retry once or twice with backoff by continuing the same run in its retained worktree, before declaring `failed` (reuse #923's `--continue-run` resume; #1278 made the recorded branch reliable). A run failing the same way after retries stays `failed`.
- [Heal main-checkout workspace links at worktree teardown](tickets/2026-07-27_main-checkout-links-into-run-worktree.md) — when a run worktree is removed, scan the project root's dependency symlinks for targets inside the removed worktree and re-point them at the root's own sibling paths. In-thread analysis assessed prevention and ruled it out (no pnpm knob; real dirs cost the disk #736 avoids) — build the recommended teardown healing only.

## Priority 5

- [Show agent error messages in red in the session log](tickets/2026-07-25_show-agent-error-red.md) — maintainer pre-authorized this for autonomous AI pickup ("only if quick-win"); bail out and report if it turns out non-trivial.
- [Highlight the user prompt on the run page](tickets/2026-07-25_where-see-user-prompt.md) — highlight the sent user prompt (the text next to `YOU`) in blue; additionally show it as the first log entry if that stays trivial ("only if quick-win").
- [Surface the prompt analysis in the dashboard](tickets/2026-07-25_show-prompt-analysis.md) — on the run page, show from the run's `ANALYSIS_RESULT.md`: Scope (one word), Variability (one word), Plan yes/no (label "Whether the work includes a plan"), New tickets yes/no (label "Whether the work spans over new tickets"). Parse tolerantly and hide fields that are absent; maintainer pre-authorized this for autonomous AI pickup ("only if quick-win").
- [Fix the empty dropdown at the bottom of the settings UI](tickets/2026-07-25_empty-settings-dropdown.md) — diagnose why an empty dropdown renders; either give it its intended content or stop rendering it (the ticket accepts both outcomes).
- [ ] [What is "Log"?](tickets/2026-07-25_what-is-log.md)

## Priority 4

- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the relay and cross-daemon federation layer (`packages/the-framework/src/relay.ts`, `src/dashboard/relay-endpoints.ts`, `src/dashboard/remote-run.ts`, `src/dashboard-rpc/relay-dispatch.ts`, `src/dashboard-rpc/relay-run.ts`)" — `relay.ts` binds `0.0.0.0` by default and is documented as deliberately unauthenticated, and `remote-run.ts` forwards a saved device's daemon token as a `Cookie` header on fetches to a user-supplied target URL (an SSRF/token-leak shape); the in-memory token map is the package's most sensitive in-process secret.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the relay and cross-daemon federation layer (`packages/the-framework/src/relay.ts`, `src/dashboard/relay-endpoints.ts`, `src/dashboard/remote-run.ts`, `src/dashboard-rpc/relay-dispatch.ts`, `src/dashboard-rpc/relay-run.ts`)"
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the `packages/ai-autopilot` runner and bootstrap layers (`src/runner/`, `src/bootstrap/`)" — `runner/local.ts` spawns agent-generated command text with `shell: true` and inherits the full host `process.env` (API tokens included) into agent-run commands, `runner/docker.ts` builds shell strings by interpolation, and the two deliberately divergent path-containment guards (`runner/path.ts` `safeSegments` vs the resolve-based `within()` in `local.ts`) must be shown to agree; first step is tracing whether the spawn sites are reachable from untrusted input.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the `packages/ai-autopilot` runner and bootstrap layers (`src/runner/`, `src/bootstrap/`)" — the package is already well-modularized (largest file 356 lines), so focus on unifying the two divergent path-containment guard implementations rather than restructuring.

## Priority 2

- [Drop readZip/ZipEntry from the public API barrel](tickets/2026-07-21_readzip-leaks-onto-public-api.md) — mark both `@internal` in `driver/actions-zip.ts` and remove the re-export at `packages/the-framework/src/driver/index.ts:29`. Nothing has shipped to npm (#746 still open) and the only importer is `actions-zip.test.ts`, which imports the module directly — the removal breaks no consumer.
- [Honor the XDG spec default for the global store path](tickets/2026-07-25_global-store-one-file-or-folder.md) — in `registryPath()`, use `~/.config/the-framework.json` even when `XDG_CONFIG_HOME` is unset (the spec default), with a migration read of the legacy `~/.the-framework.json`. Scoped to this fix only ("worth doing either way" per the ticket) — the file-vs-folder question stays open, do not restructure the store.
- [Verify Fable is selectable in the model picker; fix it if it still isn't](tickets/2026-07-25_bug-cannot-select-fable.md) — the report predates the picker's rework (the agent/model menu in `packages/framework-dashboard/components/Composer.tsx` lists `{ value: 'fable', label: 'Fable' }`); first try to reproduce, and if selecting Fable still fails, fix it; if it works now, say so with the evidence so the ticket can be closed.
- [Document metaFromEvents' updatedAt anchoring and drop the false test claim](tickets/2026-07-22_metafromevents-updatedat-replay.md) — scoped to the minimal option, sufficient while the helper has no production caller: document on `metaFromEvents` (`packages/the-framework/src/store/run-store.ts`) that replay anchors `updatedAt` to `startedAt`, and reword the "reconstructs the same snapshot as live appends" test so it stops claiming what `updatedAt` cannot satisfy. Do not add per-event timestamps — that design call waits until something actually reads `updatedAt` off a replayed meta, per the ticket.

## Priority 1

- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the CLI entrypoint `packages/the-framework/src/cli.ts`" — at 2,514 lines with 38 top-level functions it is the largest and most overloaded module in the repo: a ~330-line `parseArgs` switch over 90+ flags, a 750-line `runBuild`, plus daemon start/stop, relay launch, maintenance-run spawning, deploy targets, and worktree commands all in one file, with the package's highest TODO/FIXME density (12).
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the CLI entrypoint `packages/the-framework/src/cli.ts`" — it constructs argv for detached child processes from user input, handles `--dangerously-skip-permissions` / `--permission-mode`, and prints the non-loopback daemon token (`printNonLoopbackAccess`).
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the daemon runtime cluster (`packages/the-framework/src/daemon.ts`, `daemon-runtime.ts`, `daemon-services.ts`)" — `daemon-runtime.ts` (1,035 lines) mixes worktree teardown, run-history moves, transient-failure retry policy, and stderr log plumbing in one file.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the daemon runtime cluster (`packages/the-framework/src/daemon.ts`, `daemon-runtime.ts`, `daemon-services.ts`)" — `spawnDetached`, `terminate(pid, graceMs)`, and `startOptionFlags(options)` form the argument-construction boundary where RPC-supplied `StartRunOptions` become command-line flags for spawned processes.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the dashboard HTTP edge and auth boundary (`packages/the-framework/src/dashboard/server.ts`, `bridge-endpoints.ts`, `static.ts`, `file-read.ts`, `browser-proxy.ts`, plus `src/loopback-host.ts` and `src/request-path.ts`)" — two distinct token schemes live here (the `fw_daemon` cookie/`?token=` redirect flow in `authorizeDaemonRequest` and the bridge bearer token compared with `timingSafeEqual`), and the two path-confinement implementations differ (`static.ts` normalize+prefix compare vs `file-read.ts` two-sided `realpath`).
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the dashboard HTTP edge and auth boundary (`packages/the-framework/src/dashboard/server.ts`, `bridge-endpoints.ts`, `static.ts`, `file-read.ts`, `browser-proxy.ts`, plus `src/loopback-host.ts` and `src/request-path.ts`)" — consolidating the two differing path-confinement implementations into one shared guard is the headline opportunity.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the telefunc RPC surface `packages/the-framework/src/dashboard-rpc`" — every exported telefunction is a remotely callable entry point whose only authz is the outer HTTP token guard; `context.ts` resolves client-supplied `projectId`/`runId` into filesystem checkouts, and `fromContext` silently falls back to the global registry when context is missing (a scoping-bypass shape), while `control.telefunc.ts` starts runs, opens editors, and drives handoff.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the telefunc RPC surface `packages/the-framework/src/dashboard-rpc`" — `reads.telefunc.ts` (431 lines) and `control.telefunc.ts` (418) dominate the 14-file surface.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the preset and prompt-composition modules in `packages/the-framework/src` (`preset-catalog.ts`, `preset-prompt.ts`, `preset-registry.ts`, `presets.ts`, `project-presets.ts`, `prompt-run.ts`, `prompt-template.ts`, `system-prompt.ts`)" — five near-synonymous preset modules with overlapping exports and four different notions of "the preset list" (`PRESET_STEMS`, the `presets` object, `LAUNCHER_PRESETS`, `PROJECT_PRESETS_FILE`), plus a sixth definition (`CustomPreset`/`sanitizeCustomPresets`) in `registry.ts`; mind the import-cycle constraint documented in `preset-registry.ts`.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the preset and prompt-composition modules in `packages/the-framework/src` (`preset-catalog.ts`, `preset-prompt.ts`, `preset-registry.ts`, `presets.ts`, `project-presets.ts`, `prompt-run.ts`, `prompt-template.ts`, `system-prompt.ts`)" — custom presets are user-authored prompt text persisted to a committed file and later interpolated into agent system prompts.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the run/session shell of the dashboard UI (`packages/framework-dashboard/pages/index/+Page.tsx` and the run-workspace components `components/{RunHistory,Composer,RunView,RunHandoff,RunComposer,StartRunForm,SessionActionsMenu,PromptEditor,RightRail,DashboardPage,OptionsMenu}.tsx`); leave the vendored `components/ui/` and `components/animate-ui/` untouched so they stay re-syncable with upstream" — `+Page.tsx` is a 428-line orchestrator threading props for the whole page, `RunHistory.tsx` (613 lines) is a god-component with five private sub-components, and 36 files import telefunc RPC stubs directly with no intervening data layer, so any RPC signature change fans out across components.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the run/session shell of the dashboard UI (`packages/framework-dashboard/pages/index/+Page.tsx` and the run-workspace components listed in the paired maintainability entry)" — note the `server/*.telefunc.ts` files here are thin re-export shims, so this covers only the client half of the trust boundary; the server half is the `packages/the-framework/src/dashboard-rpc` entry.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the dashboard's device-connection and credential surface (`packages/framework-dashboard/lib/{profiles,remote-target,use-device-status,draft-handoff}.ts`, `components/{AddDeviceDialog,DevicesSettings,ConnectionIndicator,BridgeSettings,DiscordDialogs}.tsx`, `server/{devices,preferences}.telefunc.ts`)" — the device hop URL puts a bearer token in the address bar via `location.assign` (`lib/profiles.ts`), device tokens persist unencrypted in `localStorage['fw.devices']`, `parseDeviceUrl` accepts any pasted URL with no scheme/host allowlist, and `DiscordDialogs.tsx` is the write path for a Discord bot token and webhook URL.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the dashboard's device-connection and credential surface (`packages/framework-dashboard/lib/{profiles,remote-target,use-device-status,draft-handoff}.ts`, `components/{AddDeviceDialog,DevicesSettings,ConnectionIndicator,BridgeSettings,DiscordDialogs}.tsx`, `server/{devices,preferences}.telefunc.ts`)"
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the dashboard's untrusted-content rendering (`packages/framework-dashboard/components/BrowserPanel.tsx` and `components/Markdown.tsx`, with their consumers `EventList.tsx`, `DiffView.tsx`, `FilePreview.tsx`)" — `Markdown.tsx` is a hand-rolled renderer for agent-authored text whose header claims "never injects HTML" and "no javascript: smuggling" (verify the link regexes rather than trusting the comment), and `BrowserPanel.tsx` is a bidirectional bridge POSTing synthesized clicks/keystrokes to a daemon-proxied headless-Chrome session.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the dashboard's untrusted-content rendering (`packages/framework-dashboard/components/BrowserPanel.tsx` and `components/Markdown.tsx`, with their consumers `EventList.tsx`, `DiffView.tsx`, `FilePreview.tsx`)"
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the `packages/ai-sdk` core (`src/agent.ts`, `src/types.ts`, and the 45 flat modules at the `src/` root)" — `agent.ts` is 2,083 lines with a single ~500-line `prompt` method and a hand-expanded four-way loop-driver matrix (`runAgentLoop` / `runAgentLoopStreaming` / `runAgentLoopOnce` / `runAgentLoopStreamingOnce`), `types.ts` is 1,137 lines, and 10,426 LOC sit flat at the `src/` root next to well-organized subdirs.
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the `packages/ai-sdk` core (`src/agent.ts`, `src/types.ts`, and the 45 flat modules at the `src/` root, plus `src/computer-use/playwright.ts`, `src/gateway/http-gateway-adapter.ts`, and `src/agent-sse.ts`)"
- Apply .the-framework/presets/security_audit.md with tf.params.what set to "the `packages/mcp` auth and runtime boundary (`src/auth/oauth2.ts`, `src/runtime/web-handler.ts`, `src/runtime/node-handler.ts`), extended to connector credential propagation in `packages/mcp-connectors` (`mountConnectors.ts`) and skill/resource loading in `packages/ai-skills/src/loader.ts` and `registry.ts`" — this is the repo's authentication boundary: OAuth 2.1 bearer protection with a grants-everything `'*'` scope wildcard, an `X-Forwarded-Host`/`-Proto` trust gate, and `node-handler.ts` constructing the request URL from the client-supplied `Host` header; the security reasoning is written down in-comment and is therefore testable.
- Apply .the-framework/presets/maintainability.md with tf.params.what set to "the `packages/mcp` auth and runtime boundary (`src/auth/oauth2.ts`, `src/runtime/web-handler.ts`, `src/runtime/node-handler.ts`) together with the near-identical `client.ts` fetch wrappers duplicated across `packages/mcp-connector-github` and `packages/mcp-connector-google-drive`" — ~50 lines of error/fetch wrapper are duplicated per connector and will scale linearly with connector count, but both connector packages are deliberately zero-dependency, so weigh hoisting into `@gemstack/mcp-connectors` as a tradeoff rather than an obvious win.

## Unprioritized

- [Add the workspace-boundary instruction to the run system prompt](tickets/2026-07-27_agent-escaped-run-worktree.md) — scoped to the ticket's fix direction 2 only, which it calls "cheap and worth doing regardless": instruct the agent that all reads, writes, and git operations stay under the run's working directory (no absolute paths that reach above it). Add it as a protocol string in the runtime append layer (`packages/the-framework/src/system-prompt.ts` composition, protocol strings beside AWAIT/SIGNAL) — do not touch the drift-guarded `prompts/system_prompt.md`. Directions 1 (move worktrees outside the repo) and 3 (teardown tripwire) are larger and stay out of this entry.
- [Landing page: add feature card "Bring Your Own Prompts"](tickets/2026-07-26_landing-bring-your-own-prompts.md) — add one card to the Features grid in `packages/the-framework.ai/pages/index/Features.tsx`, following the existing card pattern, with the ticket's copy: "Save and quickly re-use your favorite prompts. Share some per-project with your teammates, while saving others as personal prompts."
- [Landing page: add feature card "Headless Browser"](tickets/2026-07-26_landing-headless-browser.md) — add one card to the Features grid in `packages/the-framework.ai/pages/index/Features.tsx`, following the existing card pattern, with the ticket's copy: "The Framework launches a browser (Chromium) headless, giving agents full seamless access (e.g. DOM via Chrome MCP). No need for your AI browser extension anymore."
- [Landing page: add feature card "Optimal Quota Usage"](tickets/2026-07-26_landing-optimal-quota-usage.md) — add one card to the Features grid in `packages/the-framework.ai/pages/index/Features.tsx`, following the existing card pattern, with the ticket's copy: "Stop wasting unused quota — The Framework drives AI autonomy for optimal quota usage: maximum daily usage while keeping enough quota space for manual prompts."
- [Teach the queue readers to skip a "Not queued" section](tickets/2026-07-27_queue-no-format-contract.md) — scoped to the ticket's short-term fix direction: bullets under a heading named `Not queued` are prose, never entries. Implement the skip in BOTH readers that must agree (`parseTodoItems` in `packages/the-framework/src/dashboard/queue.ts` and the sweep's `parseTodoEntries` in `todo-loop.ts` — the #1296 pairing), teach the triage presets (`prompts/presets/triage_quick.md`, `triage_consensual.md`) to write exclusions only there, and the drain preset to refuse an entry whose text says it must not be worked. The full format contract (direction 1, checkbox/heading-gated entries) stays open.
