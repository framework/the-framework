Tests for `bootstrap.ts` — covers the offline capstone end-to-end: Vike preset detection, all 3 worker files landing in the sandbox, the 2-pass loop reaching production-grade with no blockers, the SSR→Cloudflare deploy through the real `cloudflareTarget` (asserting the simulated `workers.dev` URL), scope-first/done-last event order plus terminal lines, and the scale-mode overview.
