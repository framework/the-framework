The published family: the product (`the-framework`), its dashboard UI (`framework-dashboard`), the claude.ai browser extension (`chrome-extension`), and the product's website (`the-framework.ai`). Dependencies point one way — dashboard → product — and nothing depends up.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
