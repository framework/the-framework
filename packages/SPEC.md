The published family: the product (`the-framework`), its dashboard UI (`framework-dashboard`), the orchestration engine (`ai-autopilot`), the agent runtime (`ai-sdk`), the claude.ai browser extension (`chrome-extension`), and the product's website (`the-framework.ai`). Dependencies point one way — dashboard → product → orchestration → runtime — and nothing depends up.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
