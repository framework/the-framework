The package's source: the driver contract, the process engine every local driver shares, and the four implementations that ship with the package. See the package `SPEC.md` for how they relate; each module's `*.SPEC.md` holds its business logic.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
