The entry point: re-exports everything a program imports from the package, the scheduler among them: the names, the run records and markers with the tool's mark, one run and its resume with the run's id rule and the prompt line a follow-up adds, the sweep, the processes (a run and a resume of the real project, the detached run and its spawn, the detached continuation, whether a run can start here, the coding agents and their names, the `AGENT_ID` environment name), the probe of a live pid, the run's lock path and the spawned run's stderr path (`run-lock.ts`), the project's git host (a branch's pull request, a merge), the live record, the dashboard's hooks writer (`init`, and the writer another tool's `init` uses) and the command line. No rule of its own.
