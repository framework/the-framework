What the tests cover, against real files (the merge into a person's file is the writer's, covered in `agent-runner`):

- **A fresh file** - a project with the dashboard's directory and no hooks file gets the four keys, `open`, `close`, `offset` and `switch`, and the file reads back as exactly this tool's lines.
