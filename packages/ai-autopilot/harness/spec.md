Opt-in, out-of-band verification harnesses for capabilities that cannot run under the package's normal `node --test` suite.

## TLDR

- `webcontainer/` — headless-Chromium boot-and-serve proof for `WebContainerRunner` (browser-only; needs a Chromium and network).
