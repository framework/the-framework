The harness's driver: launches a headless browser, loads the harness page, waits for its checks to finish, and passes or fails on the result — a headed mode instead keeps a visible window open to inspect the live preview.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
