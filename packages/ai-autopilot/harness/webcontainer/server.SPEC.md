The harness's local web server: hosts the test page with the cross-origin isolation protections a WebContainer requires, and serves the compiled adapter and the WebContainer library from the same origin so the page can load them.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
