Tests for `deploy.ts` — covers `planOnlyTarget` (never deploys, "infra-gated" detail), `FakeDeployTarget` plan recording, and `agentDeploy` over `AiFake`: structured decision reaches the target, plan-only is the default when no target is wired, an out-of-set target (`heroku`) normalizes to the first allowed one, and a custom `targets` list both steers the plan and reaches the model prompt.
