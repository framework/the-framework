Tests for `dokploy.ts` — via a recording fake fetch: correct `application.deploy`/`application.redeploy` endpoint and `x-api-key` header, serverUrl normalization (`/api/` suffix), token fallback to `DOKPLOY_AUTH_TOKEN` (env save/restore), and the no-throw failure paths (missing token/applicationId short-circuit with zero requests, non-2xx surfaces status + body, a network rejection is caught).
