Proves the boot-and-serve check end-to-end inside a real Docker container — install, serve, and health fetch, plus the error, boot-crash, and failed-install blockers — skipping cleanly when no Docker daemon is available.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
