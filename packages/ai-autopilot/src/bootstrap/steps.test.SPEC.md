Covers the default step wirings — the Supervisor-backed build narrates its progress, the loop-backed checklist reads the production-grade verdict (a missing one is itself a blocker), improve fires the review chain — and the whole bootstrap flow end-to-end over these real primitives, offline.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
