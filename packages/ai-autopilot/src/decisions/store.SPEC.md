Saves and loads the ledger as a DECISIONS.md file at the project root — a missing file simply means an empty ledger, the normal first-run state — through a tiny filesystem seam that works the same on the host disk and inside a sandboxed workspace.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
