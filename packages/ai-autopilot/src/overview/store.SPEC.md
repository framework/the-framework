Reads and writes the overview's CODE-OVERVIEW.md file through a pluggable storage slot — a missing file just means the map was never generated — so the map persists on the host disk and inside a sandboxed session alike.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
