Turns an agent into the supervisor's planning stage: the agent is asked to break the task into the smallest set of independent subtasks that can run in parallel, answered as a structured list — each subtask saying what to do and optionally which worker should do it — so the agent supplies the planning judgment and autopilot only orchestrates what it returns.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
