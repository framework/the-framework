Covers preset composition — check chains accumulate, same-id prompt bodies are overridden by the later preset, the last declared default change kind wins, the result is itself composable — and picking a preset by name.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
