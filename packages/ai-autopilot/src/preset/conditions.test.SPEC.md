Covers mode-variant resolution: the base file wins when no mode is active, an active-mode variant replaces its base, a variant whose mode is inactive is ignored, the most specific eligible variant wins, and each file resolves independently.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
