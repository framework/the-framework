Covers that defining a preset yields a frozen, complete bundle (title falls back to the name, contents default to empty, the default change kind is optional) and that a missing or malformed name is rejected.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
