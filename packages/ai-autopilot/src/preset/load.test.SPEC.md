Covers loading a preset from a folder of markdown: identity and the optional default change kind come from the manifest, mode variants replace their base files only under an active mode, a folder without a manifest is rejected, and a missing content subfolder simply means no content.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
