Tests for `load.ts` — covers loading `{loops, prompts}` from a tmpdir fixture (manifest fields, `metadata.on`/`run` loops, prompt bodies), mode behavior (base-only with no mode; a `conditions: technical` variant replacing its base under `modes: ['technical']`), `DomainPresetError` on missing `preset.md`, `loadLoopsFrom` on a missing directory, and `metadata.event` → `defaultEvent` (absent when unset).
