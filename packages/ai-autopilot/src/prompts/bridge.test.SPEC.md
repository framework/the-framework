Covers the bridge from prompt data to runnable checks: the decisions briefing is prepended only when something was rejected, the change event renders into task text, a fresh agent is built for every pass, and a materialized library resolves the loop's ids end to end.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
