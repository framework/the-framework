Covers prompt parsing: sensible fallbacks (id and title fall back to the name, one pass by default), the optional dispatch id, title, passes, and change kind, and rejection of empty bodies, bad pass counts, and malformed ids.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
