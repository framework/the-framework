Covers only what runs outside a browser: the availability check reports false in plain Node, booting there fails with a clear message, and constructing the runner never pulls in the browser-only dependency — the real boot-and-serve proof lives in the browser harness.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
