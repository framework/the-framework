Tests declarative conversation auto-persist: per-class opt-in, thread creation and resumption per user and agent class, per-call and explicit-chain overrides, and the ownership check that stops one user resuming another's thread while ownerless threads stay resumable.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
