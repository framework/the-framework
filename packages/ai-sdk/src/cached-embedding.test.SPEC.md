Tests the embedding cache: only misses reach the provider, reported usage is what was actually charged (zero on full hits), keys include the model, and the oldest entries are evicted at the cap.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
