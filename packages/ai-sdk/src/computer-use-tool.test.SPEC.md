Tests the computer-use tool wrapper: it accepts only Anthropic-served models, is marked so adapters recognize it, gates every action behind approval by default, returns screenshots as images, keeps cursor state per instance, and enforces a per-instance action cap.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
