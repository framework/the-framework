Covers post-turn memory extraction: confidence-threshold filtering, tag merging, silent skips when extraction doesn't apply, error isolation from the parent run, and the automatic install when an agent opts in — but not on continuations or failed runs.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
