Covers the OpenAI-compatible provider family: each reports its own name and correct default endpoint, an explicit endpoint or key wins over defaults, and Azure uses the caller's endpoint verbatim.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
