Tests for the OpenAI-compatible provider wrappers (xAI, Groq, DeepSeek, Ollama, Azure) — each reports its `name` and defaults its base URL (`api.x.ai/v1`, `api.groq.com/openai/v1`, `api.deepseek.com/v1`, `localhost:11434/v1`); explicit `baseUrl` wins; api key forwarded; Ollama substitutes the placeholder key `'ollama'` the SDK accepts; Azure uses the caller endpoint verbatim with no default.
