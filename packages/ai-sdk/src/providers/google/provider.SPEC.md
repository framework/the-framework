Google's entry point: one configuration, and a specialist handed out per capability — Gemini chat, embeddings, Imagen images, hosted files, and hosted document stores; only chat receives the shared cache registry, since only chat does prompt caching.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
