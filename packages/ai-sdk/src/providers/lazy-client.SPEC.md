Shared helper that builds a vendor's client once, on first use, so every vendor SDK stays an optional install paid for only when its provider is actually used.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
