Builds the OpenAI SDK client on first use — so the SDK stays an optional install — pointed at whichever OpenAI-compatible service the configuration names.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
