The neutral contracts an application registers so queued agent prompts can be dispatched to its job queue and, optionally, stream progress to its broadcast channels — the package ships no queue of its own.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
