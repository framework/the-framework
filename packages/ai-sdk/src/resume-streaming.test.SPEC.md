Covers streaming while a paused sub-agent resumes: progress updates via the default or a custom projector, silence when streaming isn't requested, re-pausing mid-resume, and batch resumes tagging every update with the sub-run it came from so hosts can fan updates out per sub-agent.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
