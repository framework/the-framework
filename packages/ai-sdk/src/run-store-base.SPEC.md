The shared storage machinery behind both paused-run stores (top-level runs and sub-agent runs): a simple in-memory version and a bring-your-own-cache version with a time-to-live, so both families persist snapshots the same way — store, peek, and consume-once.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
