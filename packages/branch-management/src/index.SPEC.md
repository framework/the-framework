The package's single entry point: it gathers the git runner, the naming and layout conventions, the checkout lifecycle, the dependency linking, the branch-name links and the reclaim rule into one place for a caller to import. No business logic of its own.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
