The run view's banner for a session running on GitHub Actions: it explains that updates arrive in a burst when the run finishes — so the quiet feed reads as expected, not stalled — and links to the live Actions run; any other target renders nothing.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
