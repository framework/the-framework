The strip under the launcher naming the settings the next session will actually run with — the same list the options gear shows, plus values from the repo's committed config, marked as the repo's rather than yours since a value you never chose is the part worth seeing.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
