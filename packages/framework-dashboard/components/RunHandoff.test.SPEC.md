Covers the handoff: the summary of what a finished session produced (commits and files behind the disclosure), honest reasons instead of dead buttons (nothing committed, uncommitted files named, branch gone, no remote), one action naming the outcome — Open PR, becoming Merge PR once a PR exists and nothing once it landed — surfaced failures, and the single arm-the-handoff checkbox whose wording always matches what the session will actually do.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
