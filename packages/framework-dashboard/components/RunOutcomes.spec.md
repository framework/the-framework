Stacked outcome bar + legend for how past runs ended (#471): Done/Failed/Stopped counts as proportional segments in the reserved status colors, with a written label+count legend so identity is never carried by color alone; zero-count outcomes drop from the bar but stay listed. No `title` tooltips — the legend already names everything, and a system tooltip would be the one slow tooltip in a dashboard of instant ones (#1149).
