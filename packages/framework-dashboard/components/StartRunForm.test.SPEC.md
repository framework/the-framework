Covers the submit semantics — presets run unattended, typed prompts stay attended — and the preflight warnings: web-run trust, agent-CLI readiness (with the GitHub CLI half asked only while publishing is armed, and never for an Actions run), the Haiku advisory, and the auto-merge-disabled warning, all teaching without ever blocking the start.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
