A state helper that lets a component run controlled (the parent owns the value) or uncontrolled (it owns its own), giving the component one code path either way.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
