The shell every page renders inside, together with the app's global stylesheet: the shell applies the user's theme choice and catches crashes, and the stylesheet defines the shared visual identity — the light and dark palettes on the landing page's colors so dashboard and website read as one product, plus the status vocabulary where each meaning (done, failed, stopped, informational, live) has exactly one color.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
