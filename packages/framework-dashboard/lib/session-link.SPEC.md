A session's "Open session" link, offered only when the configured URL genuinely encodes that session's id — the generic entry page would open the product, not the session, so a link that cannot open this session shows nothing instead.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
