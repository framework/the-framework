The one way both composers start a session, so the daemon's refusal to run two sessions on the same checkout reads the same on either surface — reworded for a person as "a session is already active for this project" rather than the daemon's own log phrasing.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
