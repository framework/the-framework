Re-export shim (#405) pinning the baked RPC key `/server/control.telefunc.ts` for the steering telefunctions — `sendStop`, `sendChoice`, `sendBridgeAnswer(Cancel)`, `sendMessage`, `sendSetHandoff`, `sendStart`, preview (`sendPreview`/`onServeTargets`/`sendStopPreview`/`onPreviewStatus`), `sendOpenInApp`, worktree/session removal, `sendPushBranch`, `sendOpenPullRequest`, `sendQueueTicket` — implemented in `@gemstack/the-framework/dashboard-rpc` so the daemon serves them in-process. Imported-then-exported, not `export ... from` (#1014, see `server/.spec.md`).
