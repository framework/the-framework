Re-export shim (#410) pinning the baked RPC key `/server/preferences.telefunc.ts` for the preferences telefunctions — user prefs (`onPreferences`/`savePreferences`/`patchPreferences`), per-project prefs and presets, `onEditors`, `onNotifyChannels`, `saveDiscordCredentials` — implemented in `@gemstack/the-framework/dashboard-rpc`, which reads/writes the same `the-framework.json` as the registry. Also re-exports related types via `export type ... from`, which is safe (#1014 only bites value re-exports — types are erased before telefunc's transform runs). See `server/.spec.md`.
