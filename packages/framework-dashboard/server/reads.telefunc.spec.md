Re-export shim (#405) pinning the baked RPC key `/server/reads.telefunc.ts` for the ~30 read-model telefunctions — runs/run detail, docs, project log, queue, overview/dashboard aggregates, recent runs, hot tickets, interventions, activity, GitHub URL, git status, project files/file status/diffs/run changes/file content, tickets (+meta/all), retained worktrees, run worktree/handoff, system prompt, and the bridge reads — implemented in `@gemstack/the-framework/dashboard-rpc` and served in-process by the daemon. Imported-then-exported, not `export ... from` (#1014, see `server/.spec.md`).
