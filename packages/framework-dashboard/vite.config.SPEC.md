Dev-server and build setup; its one behavior of note is an opt-in dev mode that brings up a real daemon and forwards the dashboard's calls to it, so starting sessions works from the live-reload UI (the plain dev server can only read).

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
