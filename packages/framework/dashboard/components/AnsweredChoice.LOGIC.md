An answered gate collapsed to one row, so what was decided stays visible without taking more of the page than a line: a green check, the question, any context the surface adds after it (on the Overview's open-questions list, which agent asked), and "Expand" or "Collapse" at the end. Expanded, it lists every option of the gate with a check on the picked ones and the rest dimmed, or the line "Accepted none" when the answer picked nothing, followed by whatever the surface adds beneath (the Overview's link to open the agent). It is shared by the Overview's list of open questions once a question is answered and by the transcript's rows for resolved gates.
