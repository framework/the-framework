What the tests cover: the dashboard's logo and name as the way home.

- The brand is a real link pointing at the Overview.
- A plain click navigates inside the dashboard instead of reloading the page.
- A modified click — cmd, ctrl, shift, alt, or middle-click — is left to the browser, so it can open a second Overview.
- The name is hidden on narrow screens while the logo stays visible and stays the way home.
- Whether any agent is working is reflected in the logo's appearance.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
