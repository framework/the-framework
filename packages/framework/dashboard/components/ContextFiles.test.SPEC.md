What the tests cover: the list disappears entirely when no file is in the Context; every picked file is listed by its full path relative to the repo; and removing a file reports that same full path, so the right file is dropped from the Context.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
