The one toggle every collapsible section under the agent controls uses ("See actual prompt sent", "Context"), so they all read the same: a button showing a chevron that points right while the section is closed and turns down while it is open, then the label, muted until hovered. The button announces whether its section is open to assistive technology, and clicking it asks the caller to flip the section; keeping the toggle in one place stops the sections' looks drifting apart.
