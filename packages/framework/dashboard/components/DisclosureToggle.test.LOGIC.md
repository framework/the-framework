What the tests cover, for the shared collapsible-section toggle:

- **Label, click and state** - the toggle renders its label as a button, one click asks the caller to flip the section exactly once, and the button announces whether the section is open or closed.
