What the tests cover: the "New preset" dialog opens with the composer's current text prefilled and saves a custom preset carrying the typed name, that prompt, and a generated identifier; saving stays unavailable until both a name and a prompt are given; with a project open, picking "This project" saves the custom preset at the project tier while the default is the user tier; with no project open the tier choice is not offered at all and the preset is always saved as the user's own; Cancel backs out without saving.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
