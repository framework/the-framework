What the tests cover:

- **No docs, no section** - a project whose docs list is empty renders nothing.
- **Docs under a "Docs" heading** - a project with a doc shows a "Docs" heading, the doc's name (`PLAN.md`) and its rendered content.
