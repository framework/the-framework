The banner at the top of the agent view for an agent running on a connected device: it names the device the agent executes on, states that its worktree, diff, and pull request live over there, and warns that the browser preview is not yet available for a remote agent. Nothing is shown for an agent running on this machine, so the agent view can always include it.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
