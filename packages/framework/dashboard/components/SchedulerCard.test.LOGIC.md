What the tests cover, with the schedulers read stubbed:

- **The status word** - not set up, off, on with the process gone ("on, not running") and on each read as their word.
- **Loading, then rows** - "Loading…" until the read answers; then each project by name with its status word, the "keep-alive" pill and the model; a project with no state file reads "not set up".
- **The last tick** - the age ("1m ago"), a decision as "<command>: <outcome>", a note after the tick, and a decision that started a run being a button that opens that agent by project and run id while a decision that started nothing is not a button.
- **No projects** - an empty read says "No projects.".
