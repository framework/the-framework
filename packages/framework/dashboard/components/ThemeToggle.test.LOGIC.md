What the tests cover:

- **Picking a theme** - choosing "Dark" from the header's theme menu stores that choice as the dashboard's one theme preference, the same one the rest of the dashboard paints from.
- **The header says which theme is on** - the control reads "Theme: Dark" on hover without the menu being opened.
- **No choice made** - an unset preference reads as "Theme: System", the default.
