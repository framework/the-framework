What the tests cover: the header's appearance control is reachable from the header itself and picking a theme stores it as the dashboard-wide theme preference; the trigger's tooltip names the theme currently on; and an unset preference reads as System, the default.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
