Provides the dashboard's button in four emphasis levels: the default filled in the primary color, an outlined one, a ghost one that only shows a background under the pointer, and a destructive one filled in the danger color for a confirmed irreversible action. It comes in five sizes: default, small, extra small, and two square icon sizes. A disabled button is faded and ignores the pointer, and every button shows the dashboard's keyboard focus ring.
