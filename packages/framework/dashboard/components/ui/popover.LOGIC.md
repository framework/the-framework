Provides the floating panel for rich content a menu cannot hold, such as the Enhanced System Prompt's checkboxes and prompt preview: it opens just below its trigger, aligned with the trigger's left edge unless the host centers or right-aligns it, looks like the dropdown menu in `dropdown-menu.tsx`, keeps its trigger lit while open, and scrolls its body within the room available around the trigger with the dashboard's own thin scrollbar.
