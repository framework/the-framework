What the tests cover:

- **One scrolled box with the content inside** - whatever a host places in the scrolling box ends up inside its scrolled region.
- **A themed bar, not the operating system's** - the scrollbar's thumb is a rounded strip drawn in the dashboard's muted foreground tone, so it stays visible on the dark canvas, and the bar is a slim vertical strip running the full height of the box's edge.
