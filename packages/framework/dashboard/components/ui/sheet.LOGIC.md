Provides the edge-anchored drawer: a modal panel that slides in along the left or right edge of the window, full height and three quarters of the window wide up to a small cap, over a darkened backdrop, with an optional header of title and description. The host decides when it is open; Escape or a click on the backdrop closes it. The sidebar in `sidebar.tsx` uses it as its presentation on a narrow screen.
