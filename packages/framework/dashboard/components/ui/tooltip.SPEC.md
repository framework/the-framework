A small explanatory label that appears beside the control the pointer is over. Tooltips open instantly everywhere in the dashboard rather than after a hover delay, and they are always painted above the menus and popovers whose entries triggered them, so a hint is never hidden behind the menu it belongs to.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
