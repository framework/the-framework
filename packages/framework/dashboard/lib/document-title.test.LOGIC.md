What the tests cover:

- **The tab title** - with nothing waiting and no project selected the tab reads "The Framework"; a count of things needing the user is prefixed in parentheses, as "(2) The Framework"; a selected project is named before the product, as "gemstack — The Framework"; both together read "(2) gemstack — The Framework"; and a count of zero is left out rather than shown as "(0)".
