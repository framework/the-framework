What the tests cover: a draft prompt arriving in the dashboard's link is moved into per-tab browser storage and stripped from the address bar while any other link parameters stay untouched; the launcher gets the draft once and the stash is cleared as it is read, so a reload does not re-seed the prompt box; and a link carrying no draft changes nothing.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
