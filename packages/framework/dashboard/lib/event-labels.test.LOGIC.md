What the tests cover:

- **The relabeled kinds** - the coding agent's own turn reads "agent", spend reads "cost", and the line carrying the session id and its link reads "resume".
- **Everything else reads as its own name** - the session opening, a question and the ending are shown unchanged.
