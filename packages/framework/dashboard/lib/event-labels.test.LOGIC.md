What the tests cover:

- **The relabeled kinds** - the coding agent's own turn reads "agent", an agent parking on the user reads "waiting", spend reads "cost", and the line carrying the session id and its link reads "resume".
- **Everything else reads as its own name** - a hyphenated kind is shown with spaces, as "system prompt" and "ready for merge", and a single-word kind is shown unchanged.
