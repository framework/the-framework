What the tests cover:

- **The tab icon follows the working state** - the animated icon is shown while an agent is working and the plain one when none is, and asking for the icon of each state names the right file.
- **One icon declaration, reused** - the icon the page already declares is repointed rather than joined by a second one, and a page that declares no icon at all gets one.
