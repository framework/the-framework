What the tests cover: no "Open session" link is offered when the agent has no session link, no session id yet, or a link that does not contain the id — including the generic claude.ai/code entry; a link is offered only when the URL genuinely encodes the session id, and it is labelled with that id.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
