Whether the dashboard is being viewed on a narrow screen — under 768 pixels wide — which is where the sidebar becomes a panel that slides in over the page instead of sitting beside it. The answer follows the window as it is resized, and until the browser can be asked the layout is treated as wide, so the first paint never assumes the narrow one.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
