Combines the conditional styling a dashboard component builds up into one set of classes, with a later utility beating an earlier one it conflicts with — so a component can override the styling it was handed instead of the two fighting.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
