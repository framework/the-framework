The dashboard's entry point: it starts the app in the browser page the daemon serves. Everything the dashboard does happens in the browser — the daemon only serves the page and answers the dashboard's calls, and the app decides what to show from the address itself.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
