# Branch management

The `branch-management` command is not available here. Create and check out the branch `tf-<SESSION_NAME>` yourself (`$ git checkout -b tf-<SESSION_NAME>`), do all the work on it, and commit it there as you go: only what you committed is published, and nothing is committed for you.
