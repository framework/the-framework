# Branch management

The `branch-management` command is not available here. Create and check out the branch `agent-<SESSION_NAME>` yourself (`$ git checkout -b agent-<SESSION_NAME>`), and commit your work to it as you go. Only what you committed is ever published — nothing is committed on your behalf.
