What the tests cover:

- **A started agent** - the Discord message names the project, says "started" and carries the agent's title.
- **Outcomes told apart** - a finished agent that succeeded is marked "✅" and one that failed "❌".
- **Batches** - several items post as one message summarizing them as "2 session updates"; an empty batch posts nothing at all.
