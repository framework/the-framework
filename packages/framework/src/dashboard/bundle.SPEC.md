Finds the built dashboard app on disk, so the daemon can serve it. There is exactly one place it can be, and it counts as present only when its entry page is actually there — otherwise the caller falls back to serving a plain built-in page instead.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
