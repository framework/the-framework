Names what the driver seam offers the rest of The Framework: the driver contract itself, the four drivers that satisfy it (Claude Code, Codex, the `actions` run target, the `web` run target), the fake driver, and reading where the account's quota stands. The Actions run target's internal archive reader is deliberately not offered, since nothing outside that driver uses it.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
