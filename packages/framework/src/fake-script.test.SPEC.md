What the tests cover: the fake driver's demo script plays the plain build with no gate by default; the single-select variant stops to ask a question The Framework can actually read as a gate, with the expected recommended option, followed by a turn that carries no gate so the agent resumes and finishes; the multi-select variant asks a checklist whose pre-checked options are the intended ones; and an unrecognised gate variant falls back to the plain build rather than failing.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
