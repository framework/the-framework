What the tests cover: the `.the-framework/.gitignore` written at install, against real git — everything under `.the-framework/` (the transient archive, the live event log, a run checkout) stays ignored on the default branch, and the ignore file itself is the one tracked thing.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
