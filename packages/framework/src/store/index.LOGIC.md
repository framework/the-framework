The store's entry point: what the daemon, the agent process and the dashboard RPCs import to persist and read agents — the store with its reads and rescues (`agent-store.ts`), the resolution of an agent id to its checkout and event stream (`agent-checkout.ts`), and the mapping between the status snapshot and the `logs` skill's run (`run-record.ts`). It carries no logic of its own.
