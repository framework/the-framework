A runs provider for tests: the finished agents of every project held in memory, answering the list, one agent with its diary, a delete and a patch the way a provider's command would, with every delete and patch recorded so a test can check what was asked. A project it was given no agents for has no provider. It carries no product logic; the store's and the checkout's tests read finished agents through it.
