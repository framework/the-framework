Fixes where the `branches` executable lives, for a caller that puts it on the PATH of a process it starts: the package's own `bin/` directory, beside its built code, so the path is the same whether the package is a workspace checkout of this repository or an installed dependency. No caller in the repository uses it: an agent runs the command through `npx`. It carries no other rule.
