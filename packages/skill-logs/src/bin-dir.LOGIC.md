Fixes where a program that starts agents finds this skill, resolved the same way from a workspace checkout and from an installed package: the directory of the `logs` executable, for the program to put on a spawned agent's PATH so that `logs` runs from the agent's shell; the skill's name, `logs`, which is the `name` in `SKILL.md`'s front matter and the directory a coding agent lists the skill under; and the package's own directory, the one holding `SKILL.md`, for the program to hand the coding agent the skill's instructions.
