What the widget's card tests stand the dashboard in with: a fake host whose commands answer from a table keyed by project and command line (a line with no answer fails, naming itself), whose starts answer a started id each, and whose every other service is a spy; plus a way to render the card inside that host with no other widget mounted. The host is handed over whole, so the card under test never reaches a real dashboard.
