The package's entry point, re-exporting what the framework uses: the queue file's name from `names.ts`, the entry reader, the queue read and the add edit of `queue.ts`, and the type of the edit's replaceable seams from `store.ts`. It holds no logic of its own; `names.ts` is also reachable as its own entry point for browser code.
