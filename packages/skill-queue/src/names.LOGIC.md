Fixes the one name the queue package hangs off: the agent queue is the file `TODO_AGENTS.md` at the root of the `agent-data` branch. Nothing here touches node, so the dashboard's browser code can name the file too. The branch itself is not named here: it is the shared branch the `agent-data` package names, a convention rather than a setting, and the skill's `SKILL.md` names that same branch to every agent.
