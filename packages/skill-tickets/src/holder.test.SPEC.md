What the tests cover: who a claim names, against a real repository — `AGENT_ID` from the environment wins over the branch, an empty value counts as unset, without it the current branch is the holder, and a detached checkout with no `AGENT_ID` yields no identity while one with it still names the agent.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
