Settings every page of the site inherits: each page is rendered to static HTML at build time, which makes the site plain files that GitHub Pages serves without a server; the browser tab reads "The Framework" unless a page sets its own title, as the pages under `banner/`, `go-to-dashboard/` and `press/` do in their own `+config.ts`; and the favicon is the logo, `/assets/logo.svg`. The description that search engines and link previews read is "Make the important decisions, let AI do the rest. The Framework turns AI agents into autonomous teammates that handle work end-to-end — while you stay in control of key decisions."
