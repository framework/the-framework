Renders the site's link-preview image as a web page: a fixed 1200 by 630 pixel composition (the Open Graph image size) with no site navigation, meant to be screenshotted by hand into `public/banner.jpg`. The composition is the landing page's pitch condensed: "Babysit AI" crossed out above "Autonomous AI", the tagline "Make the important decisions, let AI do the rest.", the three badges "100% Open Source", "100% Free" and "100% Local", and the logo with the wordmark "The Framework". It is sized to stay legible when a chat or social network shows the card at about 40% scale, with margins of at least 64 pixels all around.

Nothing on the page is interactive, and no navigation of the site leads here: the page is reached from the "Banner" section of the press page (`../press/+Page.tsx`), which also explains how to regenerate the screenshot, and the screenshot is what `../+Head.tsx` declares as every page's preview image.
