`/banner` route — the chrome-less 1200×630 Open Graph image source page.

## TLDR

- `+Page.tsx` — the fixed-size render that gets screenshotted into `public/banner.jpg` (referenced by `og:image`).
- `+config.ts` — page title.
