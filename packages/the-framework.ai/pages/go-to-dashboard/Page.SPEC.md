Explains that the dashboard runs entirely on the visitor's machine and walks through the three ways to open it — run it if installed, install it globally, or run it once without installing — each as a click-to-copy command in the visitor's chosen package manager.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
