`/go-to-dashboard` route — explains the dashboard runs locally and gives run/install/one-shot commands.

## TLDR

- `+Page.tsx` — the three command steps with click-to-copy, package-manager-aware chips (reuses the index page's PM machinery and chrome).
- `+config.ts` — page title.
