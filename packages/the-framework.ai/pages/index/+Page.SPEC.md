The landing page at `/` — the website's home page. It puts the site-wide top navigation first, then runs the sales pitch in a fixed order: the hero, the section navigation, "Stop babysitting AI", "Autonomous AI", "How it works", the feature list, "Your framework", and the closing call to action, ending with the site-wide footer.

What each section argues is described in the directory's own spec.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
