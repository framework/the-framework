The "How it works" chapter, subtitled "The Framework introduces one major building block:", which shows that building block, "Queues" (`Queues.tsx`), and, below it, the note about the prompts everything runs on (`Prompts.tsx`).
