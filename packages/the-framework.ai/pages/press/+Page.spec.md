`/press` brand page: logo swatches, naming rules, the OG banner, and brand-asset pointers.

## TLDR

- Logo: the mark is the "hexknot" (six interlocking strands forming a hexagon), shown in dark/light `Swatch`es; download `/assets/logo.svg`, more variants via the brand playground (`brillout.github.io/brand-the-framework`).
- Name: written "The Framework" (capital T, capital F); package is `@gemstack/the-framework`.
- Banner: `/banner.jpg` (1200×630, the OG size) is a screenshot of the `/banner` page; a gist with regeneration instructions is linked.
- Brand sources/generator live in `brillout/brand-the-framework`; Discord for more material. Reuses `TopNav`/`Footer`/`ui` from `../index/`.

## Facts

- Reachable by right-clicking the logo in `TopNav` (its `onContextMenu` navigates to `/press`).
