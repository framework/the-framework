The press page (`/press`): the press kit — the logo on dark and light backgrounds with its download, how the name is written, the banner image and how it is regenerated, and where the brand sources live. The page is reached by right-clicking the logo in the top navigation (`../index/TopNav.tsx`) or by its address; no link on the site points to it.

## Business logic — TL;DR

- **The press kit** (`+Page.tsx`) - the "Logo", "Name", "Banner" and "Brand assets" sections.
- **The page title** (`+config.ts`) - "Press — The Framework".
