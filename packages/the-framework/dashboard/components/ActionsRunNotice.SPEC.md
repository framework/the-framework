The agent view's banner for an agent running on GitHub Actions: it explains that updates arrive in a burst when the workflow run finishes — so the quiet feed reads as expected, not stalled — and links to the live Actions run; any other target renders nothing.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
