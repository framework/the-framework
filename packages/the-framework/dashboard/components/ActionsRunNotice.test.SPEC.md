What the tests cover: a running agent on the `actions` run target is told its events only arrive when the run finishes, while a finished one drops that line; the link to the live Actions run appears once the agent has reported the run's URL and is absent before then; nothing at all is shown for a `local` run target or when no run target is set.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
