What the tests cover: pasting a `?token=` URL saves the device — its origin and token split out of the one paste, named after the host by default — and closes the dialog; a URL with no token leaves Add device disabled and says why; a name typed into the optional field replaces the host default.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
