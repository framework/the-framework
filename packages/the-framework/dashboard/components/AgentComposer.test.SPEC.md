Covers the one composer: the three send behaviors (message a live agent, continue a finished one on its own driver, start fresh when there is no session to resume), the new-agent preset always opening its own, the slot's Stop and Resume with their no-refire and no-flicker latches, surfaced refusals instead of silent failures, and the options gear returning only once the agent ended.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
