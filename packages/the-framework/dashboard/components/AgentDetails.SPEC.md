The "about this agent" strip revealed behind the action bar's disclosure: which driver ran the agent, which model it used, and what it has spent so far — money, input and output tokens, cached tokens read and written, and how many turns it took. Token counts are abbreviated (`1.2k`, `1.2M`), and until the agent reports any usage the strip says no spend has been reported yet.

The agent's branch, its pull request and its changed files are stated in the action bar directly above, so they are not repeated here.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
