Covers the Agents card: a row opens the agent it names (not the project launcher), loading never reads as "no agents working", an agent without an intent still gets a label, and there is no Current/Recent split.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
