What the tests cover: the picker lists every other registered project and shows which ones are already in the Context; ticking one reports that project's repo path; the trigger carries the summary of what is picked; picked files are listed and each can be dropped, reporting its path; and with no file picked the Files group shows the hint pointing at the other two ways of adding one.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
