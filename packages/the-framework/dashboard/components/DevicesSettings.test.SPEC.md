What the tests cover: an empty roster says so instead of showing a bare list; each saved device is listed with its name and its URL; removing a device drops it from the roster for good; removing the device that was picked as the next agent's run target also clears that target; and removing any other device leaves the picked target and the rest of the roster untouched.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
