How a file reads anywhere in the dashboard: a changed file as its plainly colored diff, an unchanged one with line numbers, binary and cut-off files saying so — deliberately view-only, because the dashboard answers "what did the agent do" and is not an editor.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
