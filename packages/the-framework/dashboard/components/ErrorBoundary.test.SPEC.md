Covers that a crash while drawing shows a recoverable alert naming the error (with a console trace) instead of a blank screen, and that "Try again" redraws the view once the cause is gone.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
