What the tests cover: the bar reports the project's own checkout when no agent is named and that agent's worktree when one is; a worktree additionally reports its size on disk, and the size is left out entirely (no placeholder) while the server cannot price it; an agent's pull request is shown with its number and state exactly as a project's is; and nothing at all renders when there is no checkout to report.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
