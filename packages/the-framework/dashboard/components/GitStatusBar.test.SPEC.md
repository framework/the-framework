Covers that the bar reads the project checkout or, on an agent, that agent's own worktree — including worktree-only facts like size and the branch's PR — omits a size it cannot know yet, and renders nothing when there is no checkout to report.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
