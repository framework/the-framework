Covers the presets menu: built-ins load their rendered prompt, saved and project presets load verbatim, a row's delete deletes (from its own group) without loading, an empty project group is hidden, and "New preset…" appears only where a create panel exists.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
