What the tests cover: the presets menu lists the built-in presets and loads a picked one's prompt into the editor; the user's own custom presets and the open project's shared custom presets load their stored prompt verbatim; each custom preset's delete control removes it without also loading it, and a project preset is deleted as a project preset rather than as one of the user's own; the "Project presets" group disappears when the project has none; "New preset…" opens the create dialog, and that entry is absent on surfaces where no create dialog exists.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
