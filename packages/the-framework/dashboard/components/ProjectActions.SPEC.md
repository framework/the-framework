The project home's action bar: the project's git status on the left, the workspace actions on the right. Both halves are the same ones an agent's bar shows, so the two pages cannot drift apart; here they report on and act against the project's own checkout rather than any agent's worktree.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
