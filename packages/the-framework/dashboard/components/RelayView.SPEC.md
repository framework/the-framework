The shared watch page: a teammate opening a shared link sees that one agent's live feed read-only — no project rails, no steering — and an agent that has ended or a wrong link says so instead of waiting forever.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
