The strip under the launcher naming the settings the next agent will actually run with — the same list the options gear shows, with a value the repo's committed config decided marked as the repo's rather than yours, since a value you never chose is the part worth seeing.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
