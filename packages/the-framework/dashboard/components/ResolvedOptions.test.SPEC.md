What the tests cover: the "Settings:" strip is absent entirely when no option is on; it lists exactly the options that are switched on, leaving out those that are off; an option that cannot apply here is left out even when it is stored as on; and a chip whose value comes from the repo's committed `the-framework.yml` is tagged "repo" and says so on hover, while a chip from the user's own preferences carries no tag and says it is the user's setting.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
