The dashboard's banner for the per-project settings tier that was deleted: it names each repo that had one, says what it held, and gives the exact `handoff:` line to add to that repo's committed file — with the consequence of not adding it, which is that agents there publish. A repo whose block held nothing the repo file can carry is told the user's own settings apply instead; nothing retired renders nothing, so the shell mounts it unconditionally.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
