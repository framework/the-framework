Covers the rail: one fixed width across tabs, no rail without a project, the browser tab withheld where no browser exists, tab tooltips, tabs earned by content (empty reads drop tabs, nothing at all drops the rail, pending first reads do not blink, a lost tab falls back), and the launcher owning the docs withholding that tab and skipping its read.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
