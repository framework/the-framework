Covers the settings page's dropdowns: that none renders with nothing in it, and that the one list assembled at run time — the detected editors — leaves an operable row when the daemon detects none, because Auto-detect is a real choice rather than a placeholder.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
