Covers that the preview shows the really-composed prompt, transparent mode empties it entirely, the summary reads fully-enabled only when both axes are on (and never by the dot alone), the checkboxes drive their axes with transparent locking anti-laziness off, and the browser section lengthens the previewed prompt.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
