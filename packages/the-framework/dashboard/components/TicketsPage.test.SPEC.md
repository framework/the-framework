Covers the cross-project read with per-project sections (empty projects still offered their import), sorting (the server's date order kept by default, priority with newest-first ties, the choice landing in the URL), URL-mirrored filters (search, topic and claim click-to-filter, mounting under a query starts filtered, clear from the filtered-to-nothing state), and the flat list acting per-row on its own project.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
