The agent-views rail: the ad-hoc markdown the agent pushes to the side panel (plans, summaries, writeups) as first-class tabbed views — a newly pushed view selects itself, a re-shown one updates in place without stealing the selection, and each is copyable as markdown since it is exactly the text you paste elsewhere.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
