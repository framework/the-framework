Covers that an agent's folder and editor actions address its own worktree while the project home addresses the project checkout, both pages offer the same set of actions, and the editor picker stores a choice, clears back to the default, and surfaces a hand-set editor that was not auto-detected.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
