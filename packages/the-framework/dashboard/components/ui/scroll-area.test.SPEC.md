What the tests cover: a scrollable region puts the content it is given inside the box that actually scrolls; it exposes that box so a caller can scroll the region itself; and its scrollbar is the dashboard's own — a rounded thumb in the dashboard's muted tone, drawn as a slim vertical strip down the edge — rather than the one the operating system paints.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
