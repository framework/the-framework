The dashboard's browser-side state and logic: everything the views compute rather than draw. Following the live event stream and deriving each agent's current state from it; polling the daemon's projections; the URL-as-selection routing; user preferences and saved device profiles; browser notifications; and the small pure rules the views share (labels, status tones, date formatting, ticket filtering and priority, quota bars).

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
