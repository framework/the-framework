What the tests cover: the browser tab title is the bare product name when nothing needs the user and no project is selected; it gains a leading count while something needs the user, gains the selected project's name before the product name, shows both together, and drops the count again once nothing needs the user.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
