What the tests cover: the four jargon event kinds are renamed to plain words (driver turn reads "agent", waiting-for-you reads "waiting", spend reads "cost", the resumable session reads "resume"); every other multi-word kind keeps its own name with hyphens turned into spaces; and a single-word kind is shown unchanged.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
