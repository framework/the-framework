The single color vocabulary for an agent's status, shared by every dashboard list that shows one, so the same status can never read as two different colors. A running agent takes the accent color, a done agent the success color, a stopped agent the warning color, and a failed agent the danger color; any other status is left uncolored.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
