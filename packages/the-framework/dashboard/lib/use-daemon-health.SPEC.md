Probes the daemon every few seconds so the shell can say out loud when it is unreachable — otherwise a dead daemon is invisible, since polled panels keep their last answer and the live feed retries silently; once it answers again, everything recovers on its own.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
