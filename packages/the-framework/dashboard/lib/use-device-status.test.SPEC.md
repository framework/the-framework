What the tests cover: a reachable device shows as online and an unreachable one as offline; the browser hands the daemon each saved device's URL and token so the daemon can do the ping; and with no saved devices nothing is checked at all and the status stays unknown.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
