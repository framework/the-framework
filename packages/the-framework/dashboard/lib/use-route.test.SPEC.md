What the tests cover: the dashboard reads which project and agent it is showing from the live address bar; navigating updates the address bar and the dashboard together; each navigation is a history entry so Back returns where the user was; a navigation can be asked to replace the current entry instead of adding one; going to where the user already is adds no entry at all; and the dashboard follows Back and Forward when the browser drives them.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
