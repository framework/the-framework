App-wide page setup: the dashboard is a client-only single-page app whose shell is prerendered into plain static files the daemon can serve without rendering anything itself, carrying the product title and the brand mark as the tab icon.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
