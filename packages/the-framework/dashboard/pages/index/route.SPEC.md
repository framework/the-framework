Routes every address to the single page; the page reads the address itself, since anything matched here would be frozen at what the prerendered shell was built with.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
