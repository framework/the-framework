The tests pin the shim files' shape: each must import and then export its functions — the direct re-export form used to break every dashboard call in development — and must export exactly the names it imports.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
