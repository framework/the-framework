The maintainability preset: the agent refactors whatever the user named — the launcher asks "what to refactor for maintainability" — to make it as maintainable as possible, looking for maintainability red flags and fixing them. Deliberately minimal, so its results can be judged before the prompt is made more explicit.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
