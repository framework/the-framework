Copies the dashboard's prerendered bundle into this package before publishing — the dashboard is developed in a separate unpublished package, so its assets must ride along for an installed framework to serve them; a missing bundle skips (the dashboard falls back to the basic built-in page) rather than failing.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
