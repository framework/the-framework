Covers the branches view: a link per checkout named as its branch, a rename settling in one pass (old name dropped, new one created), reclaimed checkouts losing their link, detached and slash-named branches getting none, user files and foreign symlinks never touched, the repo-root shortcut created once without clobbering, and the daemon pass visiting every project.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
