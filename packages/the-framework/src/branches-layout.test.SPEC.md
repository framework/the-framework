Covers the flat-branches layout pass: an old-location checkout is moved under its branch name, a running agent's checkout is left alone, a refused move is kept and reported instead of thrown, the root `branches` shortcut is created once and never clobbers an existing path, a settled project is a silent no-op, and the daemon pass visits every registered project.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
