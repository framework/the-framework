Covers the shared CLI runner: a killed process reports as a timeout naming the command and the budget it outran, a plain failure does not, and a per-operation budget gives slow operations room a quick one does not get.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
