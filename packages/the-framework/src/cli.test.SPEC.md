Covers the command line end to end: the four options it accepts and the flags and verbs it now refuses, reading an agent's options off its spec, running no preflight of its own, config layering (the spec beats the repo file, including turning modes off), the guards-not-in-force notices, session-link defaults, who can steer an agent and who keeps it open for chat, log entries for agents that never finished, the quality follow-up's queueing and its recursion guard, branch renames once the agent names its session, continuations re-entering the flow their record names, and failing cleanly before a driver exists.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
