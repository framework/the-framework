The browser-safe entry for the dashboard: only pure work — event formatting, agent-view derivations, prompt and preset rendering, the idle-routine list, preference defaults, and the shared notification and credential rules — so the dashboard runs the same logic the daemon does without pulling any machine-touching code into its bundle.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
