Walks the compiled import graph from the browser entry and fails on anything reaching Node-only code, so the dashboard bundle cannot silently grow a server dependency.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
