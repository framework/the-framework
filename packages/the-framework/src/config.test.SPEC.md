Covers the repo config file: parsing the preset, modes and toggles with type errors named per field, empty documents as nothing, reading the file from a directory, and a malformed file becoming a warning rather than a failed agent.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
