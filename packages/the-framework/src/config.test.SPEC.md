Covers the repo config file: parsing the modes and the publish rung with errors named per field, retired spellings ignored rather than translated, empty documents as nothing, reading the file from a directory, and a malformed file becoming a warning rather than a failed agent.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
