Covers the concurrent-agents setting end to end: the number stored on disk is the number of unattended agents the auto-PM sweep fans out, one pinned queue entry each, whether the sweep fires on schedule or from the dashboard's run-now click with auto-run off.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
