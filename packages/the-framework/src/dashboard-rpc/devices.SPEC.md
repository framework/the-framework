Health-checks the user's saved devices: the browser hands each device's address and token to the daemon, which pings it and reports who answered — the tokens are used for the check and never stored.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
