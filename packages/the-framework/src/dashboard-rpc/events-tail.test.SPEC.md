Covers the journal tail: replay what is logged then follow appends, detect a fresh agent rewriting the log in place, signal caught-up exactly once, and follow the journal into its archive so the ending's lines arrive exactly once — never replayed, never lost, even when the archive shows up late.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
