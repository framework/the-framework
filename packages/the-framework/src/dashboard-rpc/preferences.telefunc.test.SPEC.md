Covers that the settings calls degrade safely on a host with no store (reads fall back to the empty default, writes report they are not enabled), that a merge-write hands back the stored result for the caller to adopt, and that a failed write becomes a typed answer rather than an error.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
