Covers that every call the dashboard can make is actually registered on the server — one that is exported but forgotten fails at runtime with a bare error and nothing else, which is how a feature once shipped broken.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
