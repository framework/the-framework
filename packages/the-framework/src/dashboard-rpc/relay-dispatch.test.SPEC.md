Covers that the relay allowlist is exactly the agent-scoped read/steer/handoff surface (starting, deleting, and previews stay off it), that unknown names are refused, and that an allowed call runs against the device's own home project rather than the caller's project id.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
