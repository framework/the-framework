Plumbing that turns a live event source — a file tail, or an in-memory session replaying its buffered history then following live — into the stream a browser subscribes to, stopping the source when the viewer leaves and closing immediately when there is nothing to stream.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
