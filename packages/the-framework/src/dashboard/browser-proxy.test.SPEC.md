Covers the preview relay: recognizing its routes and leaving everything else to the web app, streaming frames and forwarding input to the agent, surviving malformed addresses instead of crashing the daemon, and answering "not found" for an agent without a preview or an error when it just died.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
