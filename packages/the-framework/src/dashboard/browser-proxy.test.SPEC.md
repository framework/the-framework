Covers the preview relay: recognizing its routes and leaving everything else to the web app, streaming frames and forwarding input to the run, surviving malformed addresses instead of crashing the daemon, and answering "not found" for a run without a preview or an error when the run just died.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
