The one extension→content-type table shared by the framework's two static servers (dashboard bundle in static.ts, project Preview in preview.ts) so they cannot drift apart again (each had been missing extensions the other listed); unknown extensions fall back to `application/octet-stream`. Only the lookup is shared — each server keeps its own policy (SPA fallback + immutable cache vs 403 + stream).
