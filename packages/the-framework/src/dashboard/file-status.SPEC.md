Reads each changed file's state — untracked, modified, or deleted — from git, so the dashboard's file tree can dot them; renames show under their new name, and a non-repo reads as no changes.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
