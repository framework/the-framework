What the tests cover: each changed file is reported as untracked, deleted, or modified — with additions and renames both counting as modified, and a rename reported under its new path; a directory that is not a git checkout yields "nothing changed" instead of an error.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
