The tests cover the daemon-to-daemon calls (token cookie, no Origin), device refusals and unreachability surfacing as clean results, reassembling the event stream even across split lines, a rotated token ending the stream as a normal finish, and the relayed-agent bookkeeping — list rows that mirror the device, targets that outlive the stream, and disposal clearing everything.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
