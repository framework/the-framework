Serves the prebuilt dashboard app as plain files, answering every unknown or directory-escaping path with the app shell so client-side routes always boot and nothing outside the bundle can ever be read.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
