Covers the agent lifecycle journeys: starting a publish-nothing agent and watching it live through to the archived row (checkout kept with nothing on origin, record on its own branch), two agents running concurrently each in its own checkout, and publishing a push-armed finished agent's branch to a real remote — even when the click lands mid-teardown, after which its worktree retires.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
