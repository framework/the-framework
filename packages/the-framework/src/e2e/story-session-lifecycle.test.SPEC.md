Covers the agent lifecycle journeys: starting an agent and watching it live through to the archived row (worktree retired, record on its own branch), two agents running concurrently each in its own checkout, and publishing a finished agent's branch to a real remote — even when the click lands mid-teardown.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
