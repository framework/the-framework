Formats a byte count as a short human-readable size for labels — "how much would removing this give back, at a glance" — with a dash for an unknown size so a failed measurement never reads as zero.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
