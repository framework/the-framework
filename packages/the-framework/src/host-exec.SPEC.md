Runs shell commands on the host inside the workspace the agent just built — what lets a real deploy target install, build, and publish that code — reporting output and exit code as a result, never a throw.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
