Covers the worktree sweep: every retained checkout offered to the one rule whatever the agent did, live agents and ones the daemon has not finished retiring untouched, a checkout it could not reclaim reported with its reason — once per checkout rather than once per pass, and again after a removal if a same-id checkout reappears — a remote-less repo answered from one remote check with no per-checkout probing, and — against real git with a real remote — that a removed checkout's work is on the branch and on the remote first, while a repo with nowhere to push keeps everything.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
