The one real file-system implementation behind every module's narrow file-system seam, loaded lazily so browser-reachable code can share those modules as long as no read actually runs there.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
