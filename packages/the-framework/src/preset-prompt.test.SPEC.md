Covers preset definition: the default target resolves to the launching session else the whole codebase, an explicit target wins over it, and a preset's prompt can read the settings and other presets' file locations without ever throwing.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
