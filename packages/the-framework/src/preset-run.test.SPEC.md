Covers running a build with a domain preset: the preset drives the run and is narrated (its system prompt and active modes surfaced for transparency), its review loop actually dispatches through the agent, and the lifecycle signal protocols still reach the agent even with the built-in prompt turned off.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
