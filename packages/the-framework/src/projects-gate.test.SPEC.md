Covers a project-less topic session binding itself to a project mid-run: the register and bind gates, approval (auto-accepted when headless) and decline paths, path validation, the registered-project list injected into the session's context, and the bind traveling onto the run's record.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
