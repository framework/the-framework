Tests that queue promotion skips safely (retrying only when a human is mid-edit), that landing a pinned entry is additive so concurrent agents compose and never resurrect retired work, and that claim detection spans live agents, open pull requests, and other machines' queue diffs — with slow lookups counting as claimed.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
