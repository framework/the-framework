Tests the registry's forgiving reads (malformed files, and shapes and key spellings it no longer writes), the sanitization and round-tripping of every preference kind, tiered project overrides and patch merges, atomic serialized writes that survive crashes and concurrency, and the owner-only handling of the daemon token and credentials.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
