Tests the registry's forgiving reads — a file that is missing, malformed, or in a shape or spelling it no longer writes is simply empty — the sanitization and round-tripping of every preference kind, patch writes that touch only the settings they were given, atomic serialized writes that survive crashes and concurrency, and the owner-only handling of the daemon token and credentials.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
