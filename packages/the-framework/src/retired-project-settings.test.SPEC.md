Covers the boot read of the deleted per-project settings tier: a project named by its path with the keys its block held, a block that set publishing recognized in either spelling while only a real rung is quoted back, a project since removed from the registry still named by its id, every unreadable or absent shape as nothing at all, the boot snapshot outliving the erasure of the block it came from, and the log notice carrying the same per-repo advice the banner shows — and nothing at all for nobody.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
