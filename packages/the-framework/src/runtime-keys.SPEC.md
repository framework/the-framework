The one shared naming scheme for the daemon's in-memory per-project state, so an entry can belong to a whole project or to one of its sessions and cleanups can find everything a project owns.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
