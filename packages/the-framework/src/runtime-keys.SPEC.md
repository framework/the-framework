The one shared naming scheme for the daemon's in-memory per-project state, so an entry can belong to a whole project or to one of its agents and cleanups can find everything a project owns.

## Before modifying/creating SPEC.md files

Always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
