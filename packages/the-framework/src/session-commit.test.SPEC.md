Covers the background committer that puts chat transcripts and session records into git: staging scoped to its own paths only (never the user's work), commit messages that count what landed, waiting out bursts still being written and busy repos (locks, rebases, merges — resolved correctly through linked worktrees), and leaving out a path with nothing under it so a project without chats never errors.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
