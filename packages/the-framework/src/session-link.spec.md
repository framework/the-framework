The `--session-link` plumbing: `{sessionId}` placeholder detection (`hasSessionIdPlaceholder`) and substitution (`resolveSessionLink`) for turning a link template into a real session URL, plus `CLAUDE_CODE_SESSION_LINK` — the generic `https://claude.ai/code` entry point, surfaced only as an "Open Claude Code" affordance because a headless run has no per-run deep link to build (#214).
