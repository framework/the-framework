The tests cover the suspend record: it round-trips (malformed entries are dropped, never thrown), only recent-enough entries are resumable, and a session's queue claim survives the round-trip byte-for-byte.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
