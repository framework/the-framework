The tests cover finding and linking the dependency trees (workspace packages included, never descending into the trees themselves), leaving an existing install alone, surviving a refused link, and — against real git — that the exclude rule actually hides the linked trees from a worktree's status.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
