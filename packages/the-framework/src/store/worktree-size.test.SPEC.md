The tests cover the checkout size read: the reported number converts correctly, and every failure — missing tool, unreadable path, unparseable output — reads as unknown rather than an error, since the number only labels a delete button.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
