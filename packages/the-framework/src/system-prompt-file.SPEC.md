Reads the user's own `SYSTEM.md` from the project root — the one disk-bound half of the system prompt, kept apart from the composition itself so the dashboard can render the composed prompt in the browser.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
