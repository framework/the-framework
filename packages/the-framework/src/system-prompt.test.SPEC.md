Covers the composed system channel byte for byte: the context docs and the inlined format specs, the system/user split (unmoved by a user prompt containing the split heading), vanilla keeping the emit protocols, transparent emptying the channel entirely, and the browser and hands-off additions keeping the signal protocol last.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
