Finds the workspace's queue file (`TODO_AGENTS.md`) on disk — the disk half of the ticket conventions, kept separate so the conventions themselves stay loadable in the browser.

## Before modifying/creating SPEC.md files

You must always read and respect https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
