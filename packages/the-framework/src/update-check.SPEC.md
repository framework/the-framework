After the CLI prints its version, tells the user whether a newer release is on npm — display only: any failure to reach the registry silently says nothing, and a local build ahead of the registry never reads as an available update.

## Before writing SPEC.md files

Read https://raw.githubusercontent.com/brillout/sdd/refs/heads/main/sdd.md
